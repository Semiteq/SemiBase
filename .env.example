# Role passwords for semibase. Copy to .env next to the binary and fill in.
# Precedence: command-line flag > process environment > this file.
SEMIBASE_SUPER_PASSWORD=
SEMIBASE_WRITER_PASSWORD=
SEMIBASE_PLOT_PASSWORD=
